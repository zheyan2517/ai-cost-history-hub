# Packaging status

AI Cost History Hub `v0.2.0` is distributed from source only. This repository
does not currently maintain a Homebrew tap or cask, and no Homebrew install
command should be used for this release.

When packaging is implemented, it must be accompanied by real release assets,
checksums, and a CI validation step before installation instructions are added
to public documentation.
